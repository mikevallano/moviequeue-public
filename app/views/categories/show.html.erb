<div class="row">
  <p class="pull-right"><%= link_to "All Movies", movies_path %></p>
  <h1>Category: <%= @category.name %></h1>
</div><!-- row -->

<div class="row">
  <h2>In the Queue...</h2>
  <% @category.movies.order("created_at DESC").each do |movie| %>

    <% unless movie.watchstatus %>
      <div class="movie-holder">
      <div class="movie-title">
         <p><%= movie.title %></p>
       </div><!-- movie-title -->
       <p><a href="#" data-toggle="modal" data-target="#myModal_<%= movie.id %>">
       <%= image_tag movie.imdb_artwork, class: "artwork" %></a></p>
        <div class="movie-footer-unwatched <%= 'hidden' if movie.watchstatus %>">
          <h4><%= movie.watchlist.list if movie.watchlist.present? %></h4>
        </div><!-- movie-footer -->
      </div><!-- movie-holder -->
    <% end %> <!-- unless -->

    <!-- Modal Partial - Called "Movie" -->
    <%= render :partial=>'movies/movie', :object=> movie %>

  <% end %><!-- each do -->
</div><!-- row -->

<div class="row">
  <h2>Recently Watched...</h2>
  <% @category.movies.order("updated_at DESC").each do |movie| %>

    <% if movie.watchstatus %>
      <div class="movie-holder">
      <div class="movie-title">
         <p><%= movie.title %></p>
       </div><!-- movie-title -->
       <p><a href="#" data-toggle="modal" data-target="#myModal_<%= movie.id %>">
       <%= image_tag movie.imdb_artwork, class: "artwork" %></a></p>
        <div class="movie-footer-unwatched <%= 'hidden' if movie.watchstatus %>">
          <h4><%= movie.watchlist.list if movie.watchlist.present? %></h4>
        </div><!-- movie-footer -->
      </div><!-- movie-holder -->
    <% end %> <!-- unless -->

    <!-- Modal Partial - Called "Movie" -->
    <%= render :partial=>'movies/movie', :object=> movie %>

  <% end %><!-- each do -->
</div><!-- row -->
